version: '3.8'

services:
  HuBERT:
    build: .
    volumes:
      - .:/app
    tty: true
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]